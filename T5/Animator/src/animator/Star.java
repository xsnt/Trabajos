package animator;
/*
import java.awt.Color;
import java.awt.Graphics;
import java.awt.Point;


public class Star {
    private int pontas;
    private Point local;
    private String path;
    private double volta;
    

    public Estrela(int edge, Point cnt, String path) {
        this.pontas = pontas;
        this.local = centro;
        this.path = path;
        volta = 1.2;
    }
   // Desenho da estrela
   /* public void draw(Graphics graphics){
        int[] pontosX = {-100, -25, 0, 25, 100, 50, 60, 0, -60, -50, -100};
        int[] pontosY = {-15, -25, -90, -25, -15, 25, 90, 50, 90, 25, -15};
        for(int i=0; i < pontosX.length; i++){
            pontosX[i] += local.x;
            pontosY[i] += local.y;
        }
        graphics.setColor(Color.Cyan);
        if(pontas%2 == 0)
            graphics.fillPolygon(pontosX, pontosY, pontas*2+1);
        else{
            graphics.fillPolygon(pontosX, pontosY, pontas*2);
        }
    }
    
    
    
}*/
    /* Isso provavelmente não estará no programa pelo fato de eu não saber como mover ela q_q */

